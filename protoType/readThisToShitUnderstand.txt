Srijan : 
	okay so first i init npm into my folder then follow it by initing typescript and chrome stuff using 'npm install -D typescript @types/chrome tsx' then use npx to init tsc
	then i editted the tsconfig.json we get a default is in tsconfig.json.bak for backup if we mess it up 
	now every chrom extension has a blueprint file called manifest.json it basically tells chrome what permissions your extension needs and which scripts to run. the data in that json should be self explainatory? idk the spelling
		-> if you didn't get it or you are lazy to put it in llm -_- 
			1) manifest_vesion : 3 is chome's current std for extension security
			2) permissions : we declare what our extension can touch activieTab gives acces to the current web page and scripting lets your extension run code inside the web page

	--> popup.html 
		since we have action as popup.html we will write into it this is basically yk how you get that little windows that when you click on your extension icon in chrome ? 
		**UI/UX team** if you see any .html or .css file thats your area 

	-->src/popup.ts
		1) fetch the button from popup.html
			htmmlbuttonelect t
			'HTMLButtonElement | null' — TypeScript makes sure we check if the element exists
		2) we need to perform periodic checks so async , so all clicks are listened using this function
		3) const [tab] = await line is getting currewnt activity on browser tab using chrome extension API wait i can steal info from users HEHEHE
		4) sends msg to our background workers to perform the capture
		5) the if continue of tab?.id exits before reading .id so it never crashed hopefully

	-->src/bg.ts
		this will silently run in the background of chrome as a service wroker and is repsonsible for taking screen captures when requested by popup UI
			1) the interface in ts or js is basically a set of rules like struct ishh?? ig but it also define it for the incoming popup messages atleast in this case
			2) chrom.runtime.onMessage.addListerner will listen to msg coming from popup.ts or content scripts
			3) the captureAndRedact fn captures active tab views as base64 jpeg data url inject our content script into the web page to execute DOM/visual Redaction, then send the captured image to the content script for canvas masking
			4) the Promise<void> functions marked with async always return a promise . void means that the promise resolves without returning a value.
			5) _sender in ts prefixing an unused fn para with an underscore prevents the compiler from throwing an unused variable error (fs didn't spend more than 10 mins figuring this out)
			6)chrome.tabs.captureVisibleTab() the code extension api that captures whatever is visually rendered in the viewport right now and turns into a base64 data string
				[!] if you don't know what is base64 string for it is a text format to represent binary data like pictures and what not in a specific set of 64 char  so its easily transfering store or embed in env that only accept texts
			
	--> src/content.ts
		nothing much for now just that we listen to the image sent form bg.ts
